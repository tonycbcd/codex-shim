--- a/vendor/deepseek-web-api/dist/deepseek/toolCalls.js
+++ b/vendor/deepseek-web-api/dist/deepseek/toolCalls.js
@@ -369,5 +369,10 @@ function normalizeMalformedOpeningTags(text) {
     return text.replace(/^(\s*)(tool[_-]?call|_?call|invoke)\b([^<>\n]*)>/gim, "$1<$2$3>");
 }
+function normalizeMismatchedCommandTags(text) {
+    return text
+        .replace(/<\s*_command\b([^>]*)>([\s\S]*?)<\s*\/\s*exec_command\s*>/gi, "<exec_command$1>$2</exec_command>")
+        .replace(/<\s*exec_command\b([^>]*)>([\s\S]*?)<\s*\/\s*_command\s*>/gi, "<exec_command$1>$2</exec_command>");
+}
 function normalizeDsmlTags(text) {
     return text.replace(/<\s*(\/?)\s*｜｜DSML｜｜\s*([A-Za-z_][\w:.-]*)/g, "<$1$2");
 }
@@ -625,7 +630,7 @@ function looseParameterCandidate(text, toolDefinitions) {
 }
 /** Parse model tool-call text into OpenAI-compatible calls and cleaned content. */
 export function parseToolCalls(text, seed = "tool", toolDefinitions = []) {
-    const normalizedText = withoutWrapperTags(normalizeMalformedOpeningTags(normalizeDsmlTags(text)));
+    const normalizedText = withoutWrapperTags(normalizeMismatchedCommandTags(normalizeMalformedOpeningTags(normalizeDsmlTags(text))));
     const blocks = tagBlocks(normalizedText);
     const tagged = taggedCandidates(normalizedText, blocks, toolDefinitions);
     const configured = configuredToolCandidates(normalizedText, toolDefinitions);
--- a/vendor/deepseek-web-api/dist/deepseek/toolOutcome.js
+++ b/vendor/deepseek-web-api/dist/deepseek/toolOutcome.js
@@ -1,7 +1,7 @@
 import { parseToolCalls, parseToolCallsFromParts } from "./toolCalls.js";
 export const EMPTY_TOOL_RESPONSE_TEXT = "DeepSeek Web did not produce a usable tool call or final answer after two automatic retries. Please retry this turn.";
 function hasMalformedToolProtocol(text) {
-    return /<\s*(?:_?calls?|tool[_-]?calls?|invoke|parameter|execute_command|command)\b/i.test(text) ||
+    return /<\s*(?:_?calls?|tool[_-]?calls?|invoke|parameter|execute_command|_?command)\b/i.test(text) ||
         /<\s*[^>]*\bname\s*=\s*["'][^"']+["'][^>]*>/i.test(text);
 }
 function isDeferredToolIntent(text) {
@@ -11,6 +11,7 @@ function isDeferredToolIntent(text) {
         return false;
     const chinese = /^(?:(?:我)?先|首先(?:我)?|我(?:来|会|将|准备)|接下来(?:我)?|现在(?:我)?)(?:先)?(?:看(?:一下)?|检查|查看|读取|搜索|分析|定位|确认|梳理|调查|打开|执行|运行|修改|修复|处理|验证|排查|对比|查找)/;
     const english = /^(?:let me|i(?:'ll| will| am going to)|first,?\s+i(?:'ll| will)|next,?\s+i(?:'ll| will))\s+(?:inspect|check|look|read|search|analy[sz]e|review|open|run|investigate|modify|fix|verify)\b/i;
+    const namedToolIntent = /^(?:我)?(?:将|会|准备)(?:使用|调用)?\s*[A-Za-z_][\w.-]*\s*工具(?:来|去|进行)?(?:探索|搜索|查找|查看|读取|分析|定位|检查|执行|运行|修改|修复|处理|验证|排查)/i;
     const deferredClosing = /(?:请稍等|稍等)[，,\s]*(?:我)?(?:马上|现在|接下来)?(?:会|将)?(?:开始|继续|执行|检查|查看|搜索|处理|修改|修复|验证)/;
     const stagedCommandPlan = /(?:第一步|第二步|首先|接下来)/.test(compact) &&
         /(?:^|\n)\s*(?:grep|rg|sed|cat|find|git|php|python3?|node|go|pytest|curl)\b/m.test(text);
@@ -24,6 +25,7 @@ function isDeferredToolIntent(text) {
         english.test(compact) ||
         chinese.test(finalSentence) ||
         english.test(finalSentence) ||
+        namedToolIntent.test(compact) ||
         deferredClosing.test(compact) ||
         stagedCommandPlan;
 }
