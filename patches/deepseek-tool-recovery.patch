--- a/vendor/deepseek-web-api/dist/deepseek/client.js
+++ b/vendor/deepseek-web-api/dist/deepseek/client.js
@@ -72,25 +72,34 @@
         this.logAttempt(initialRun, first.diagnostics);
         if (!first.diagnostics.recoverableEmpty)
             return { ...first, run: initialRun, retry: 0 };
-        this.logger.info("Retrying unusable DeepSeek tool response", this.logFields(initialRun, first.diagnostics));
-        const retryRun = await this.prepare(body, initialRun);
-        const second = await execute(retryRun, 1, true);
-        this.logAttempt(retryRun, second.diagnostics);
-        if (second.diagnostics.recoverableEmpty) {
-            this.logger.warn("DeepSeek tool response remained unusable after retry", {
-                ...this.logFields(retryRun, second.diagnostics),
-                fallbackVisible: true,
+        let previousRun = initialRun;
+        let previous = first;
+        const maximumRetries = 2;
+        for (let retry = 1; retry <= maximumRetries; retry += 1) {
+            this.logger.info("Retrying unusable DeepSeek tool response", {
+                ...this.logFields(previousRun, previous.diagnostics),
+                nextRetry: retry,
             });
+            const retryRun = await this.prepare(body, previousRun);
+            const current = await execute(retryRun, retry, retry === maximumRetries);
+            this.logAttempt(retryRun, current.diagnostics);
+            if (!current.diagnostics.recoverableEmpty) {
+                this.logger.info("DeepSeek tool response recovered", this.logFields(retryRun, current.diagnostics));
+                return { ...current, run: retryRun, retry };
+            }
+            previousRun = retryRun;
+            previous = current;
         }
-        else {
-            this.logger.info("DeepSeek tool response recovered", this.logFields(retryRun, second.diagnostics));
-        }
-        return { ...second, run: retryRun, retry: 1 };
+        this.logger.warn("DeepSeek tool response remained unusable after retries", {
+            ...this.logFields(previousRun, previous.diagnostics),
+            fallbackVisible: true,
+        });
+        return { ...previous, run: previousRun, retry: maximumRetries };
     }
     /** Resolve trusted request options and preserve the last good parent across an empty retry. */
     async prepare(body, retryFrom) {
         const { modelType, publicModel } = resolveModel(body);
-        const thinking = resolveThinking(body);
+        const thinking = retryFrom ? false : resolveThinking(body);
         const search = resolveSearch(body, modelType);
         const conversation = retryFrom ? null : this.sessions.resolve(body);
         const reusedSession = retryFrom
@@ -103,7 +112,7 @@
             ? null
             : buildDeepSeekPrompt(body, { reusedSession, ...(previous ? { previous } : {}) });
         const prompt = retryFrom
-            ? buildToolRecoveryPrompt(body, retryFrom.requestTurns, retryFrom.latestUserText).trim()
+            ? buildToolRecoveryPrompt(body, retryFrom.requestTurns, retryFrom.latestUserText, retryFrom.retry + 1).trim()
             : built?.prompt.trim() ?? "";
         if (!prompt)
             throw new HttpError(400, "empty input");
--- a/vendor/deepseek-web-api/dist/deepseek/promptBuild.js
+++ b/vendor/deepseek-web-api/dist/deepseek/promptBuild.js
@@ -184,25 +184,81 @@
         return typeof fn.name === "string" && fn.name.trim() ? [fn.name.trim()] : [];
     });
 }
+const RECOVERY_USER_CHARS = 6_000;
+const RECOVERY_TURN_CHARS = 12_000;
+function clippedText(value, limit) {
+    if (value.length <= limit)
+        return value;
+    const marker = "\n...[recovery context truncated]...\n";
+    const remaining = Math.max(0, limit - marker.length);
+    const head = Math.ceil(remaining / 2);
+    return `${value.slice(0, head)}${marker}${value.slice(value.length - (remaining - head))}`;
+}
+function compactRecoveryTurns(turns) {
+    const selected = [];
+    let remaining = RECOVERY_TURN_CHARS;
+    for (let index = turns.length - 1; index >= 0 && remaining > 0; index -= 1) {
+        const turn = turns[index];
+        if (!turn || turn.role === "user")
+            continue;
+        const available = Math.max(0, remaining - turn.role.length - 3);
+        if (available === 0)
+            break;
+        const content = clippedText(turn.content, available);
+        selected.unshift({ ...turn, content });
+        remaining -= turn.role.length + 3 + content.length;
+    }
+    return selected;
+}
+function compactToolDefinitions(body) {
+    const values = [
+        ...(Array.isArray(body.tools) ? body.tools : []),
+        ...(Array.isArray(body.functions) ? body.functions : []),
+    ];
+    const definitions = values.flatMap((value) => {
+        if (!isRecord(value))
+            return [];
+        const fn = isRecord(value.function) ? value.function : value;
+        if (typeof fn.name !== "string" || !fn.name.trim())
+            return [];
+        const parameters = isRecord(fn.parameters) ? fn.parameters : {};
+        const properties = isRecord(parameters.properties) ? parameters.properties : {};
+        const propertyTypes = Object.fromEntries(Object.entries(properties).flatMap(([name, schema]) => {
+            if (!isRecord(schema))
+                return [];
+            return [[name, {
+                        type: typeof schema.type === "string" ? schema.type : undefined,
+                        enum: Array.isArray(schema.enum) ? schema.enum : undefined,
+                    }]];
+        }));
+        return [{
+                name: fn.name.trim(),
+                required: Array.isArray(parameters.required) ? parameters.required : [],
+                properties: propertyTypes,
+            }];
+    });
+    return stableJson(definitions, true);
+}
 /** Retry an empty tool turn in a fresh session with only essential continuation context. */
-export function buildToolRecoveryPrompt(body, requestTurns, latestUserText) {
+export function buildToolRecoveryPrompt(body, requestTurns, latestUserText, retry = 1) {
     const names = configuredToolNames(body);
-    const instructions = instructionText(body);
-    const tools = toolState(body);
+    const strict = retry > 1;
     const policy = [
         "The previous upstream completion produced no usable tool call or final answer. Continue the same task.",
-        "After tool results, output either complete <tool_call> JSON blocks in RESPONSE or the final user-visible answer in RESPONSE.",
+        strict
+            ? "Do not analyze or explain the next step. Emit exactly one executable <tool_call> JSON block now, unless the task is already complete."
+            : "After tool results, output either complete <tool_call> JSON blocks in RESPONSE or the final user-visible answer in RESPONSE.",
         "Never end with THINK-only and never leave RESPONSE empty.",
-        "Tool arguments MUST satisfy the listed JSON schema; never emit an empty arguments object when required fields exist.",
+        "Tool arguments MUST satisfy the compact schema below; include every required field.",
         names.length ? `Available tools: ${names.join(", ")}` : "",
     ].filter(Boolean).join("\n");
     const turns = requestTurns.length > 0 ? requestTurns : requestConversationTurns(body);
+    const recentTurns = compactRecoveryTurns(turns);
     return [
-        `[Tool recovery]\n${policy}`,
-        instructions ? `[Instructions]\n${instructions}` : "",
-        tools.text ? `[Tool compatibility]\n${tools.text}` : "",
-        latestUserText ? `[Original user task]\n${latestUserText}` : "",
-        turns.length ? `[Recent conversation]\n${renderTurns(turns)}` : "",
+        `[Tool recovery attempt ${retry}]\n${policy}`,
+        latestUserText ? `[Original user task]\n${clippedText(latestUserText, RECOVERY_USER_CHARS)}` : "",
+        names.length ? `[Compact tool definitions]\n${compactToolDefinitions(body)}` : "",
+        recentTurns.length ? `[Recent non-user context]\n${renderTurns(recentTurns)}` : "",
     ].filter(Boolean).join("\n\n");
 }
 export function buildDeepSeekPrompt(body, options) {
@@ -234,4 +290,4 @@
         hasTools: tools.hasTools,
     };
 }
-//# sourceMappingURL=promptBuild.js.map
+//# sourceMappingURL=promptBuild.js.map
\ No newline at end of file
--- a/vendor/deepseek-web-api/dist/deepseek/toolOutcome.js
+++ b/vendor/deepseek-web-api/dist/deepseek/toolOutcome.js
@@ -1,5 +1,5 @@
 import { parseToolCalls, parseToolCallsFromParts } from "./toolCalls.js";
-export const EMPTY_TOOL_RESPONSE_TEXT = "DeepSeek Web did not produce a usable tool call or final answer after one automatic retry. Please retry this turn.";
+export const EMPTY_TOOL_RESPONSE_TEXT = "DeepSeek Web did not produce a usable tool call or final answer after two automatic retries. Please retry this turn.";
 function hasMalformedToolProtocol(text) {
     return /<\s*(?:_?calls?|tool[_-]?calls?|invoke|parameter)\b/i.test(text) ||
         /<\s*[^>]*\bname\s*=\s*["'][^"']+["'][^>]*>/i.test(text);
@@ -32,4 +32,4 @@
         emptyUpstream: !outputText.trim() && !reasoningText.trim(),
     };
 }
-//# sourceMappingURL=toolOutcome.js.map
+//# sourceMappingURL=toolOutcome.js.map
\ No newline at end of file
--- a/vendor/deepseek-web-api/dist/deepseek/promptBuild.d.ts
+++ b/vendor/deepseek-web-api/dist/deepseek/promptBuild.d.ts
@@ -18,7 +18,7 @@
 }
 export declare function requestConversationTurns(body: RequestBody): MessageTurn[];
 /** Retry an empty tool turn in a fresh session with only essential continuation context. */
-export declare function buildToolRecoveryPrompt(body: RequestBody, requestTurns: MessageTurn[], latestUserText: string): string;
+export declare function buildToolRecoveryPrompt(body: RequestBody, requestTurns: MessageTurn[], latestUserText: string, retry?: number): string;
 export declare function buildDeepSeekPrompt(body: RequestBody, options: PromptBuildOptions): BuiltPrompt;
 export {};
 //# sourceMappingURL=promptBuild.d.ts.map
\ No newline at end of file
--- a/vendor/deepseek-web-api/dist/deepseek/toolOutcome.d.ts
+++ b/vendor/deepseek-web-api/dist/deepseek/toolOutcome.d.ts
@@ -1,6 +1,6 @@
 /** Normalizes tool-compatible upstream text without exposing protocol garbage. */
 import type { ParsedToolCalls } from "./toolCalls.js";
-export declare const EMPTY_TOOL_RESPONSE_TEXT = "DeepSeek Web did not produce a usable tool call or final answer after one automatic retry. Please retry this turn.";
+export declare const EMPTY_TOOL_RESPONSE_TEXT = "DeepSeek Web did not produce a usable tool call or final answer after two automatic retries. Please retry this turn.";
 export interface ToolTurnOutcome {
     parsed: ParsedToolCalls;
     reasoningText: string;
